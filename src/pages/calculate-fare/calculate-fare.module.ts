import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CalculateFarePage } from './calculate-fare';

@NgModule({
  declarations: [
    //CalculateFarePage,
  ],
  imports: [
    IonicPageModule.forChild(CalculateFarePage),
  ],
})
export class CalculateFarePageModule {}
